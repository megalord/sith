(include "data/maybe")
(include "data/str")

(deftype Cons
  (* car (+ (List (Ptr Cons)) (Atom (Ptr Str)))
     cdr (Maybe (Ptr Cons))))

;; (match (car cell)
;;   ((List l) ...)
;;   ((Atom a) ...))
