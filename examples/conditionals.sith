(include "stdio.h")

(: main (-> int int char**))
(defun (main argc argv)
  (if (gte argc 2)
    (if (eq argc 2)
      (printf "%s\n" argv[1])
      (printf "%s %s\n" argv[1] argv[2]))
    (puts "no args"))
  0)
